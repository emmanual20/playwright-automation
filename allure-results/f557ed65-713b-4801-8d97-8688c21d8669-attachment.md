# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: troyGamesProject\login.test.ts >> login test
- Location: tests\troyGamesProject\login.test.ts:9:5

# Error details

```
TypeError: _login.data is not a constructor
```

# Test source

```ts
  1  | import {test,expect} from '@playwright/test'
  2  | import {data} from '../../Data/troyGamesProjectData/login.json'
  3  | 
  4  | test.beforeEach(async({page})=>{
> 5  |     let d=new data()
     |           ^ TypeError: _login.data is not a constructor
  6  | await page.goto(d.url)
  7  | })
  8  | 
  9  | test('login test',async({page})=>{
  10 |     let d=new data()
  11 |     await page.getByTestId('username-input').fill(d.usn)
  12 |     await page.getByTestId('password-input').fill(d.pwd)
  13 |     await page.getByTestId('remember-me').check()
  14 |     await page.getByRole('button',{name:'login'}).click()
  15 |     let hero=await page.locator('[class="hero"]')
  16 |     await expect(hero).toBeVisible()
  17 |     await expect(page.locator('[class="hero"]')).toContainText(d.usn)
  18 | })
```